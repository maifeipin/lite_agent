# Web Dashboard 优化 TODO 列表

## 1. UI / 品牌视觉优化
- [ ] **更换 Icon**：更新全局 Favicon，以及页面内的相关图标组件，使其更符合 LiteAgent 的整体视觉风格。
- [ ] **更换 Title**：修改 HTML 页面自带的 `<title>` 和主界面的 Header 标题栏名称，使其更具备品牌感和准确性。
- [ ] **优化“查看原文”入口 (Tile)**：调整目前邮件搜索结果卡片中“查看原文”按钮/图块(Tile)的设计。可以考虑改为体验更好的内联弹窗(Modal)、抽屉(Drawer)，或对其样式进行现代化(UI 视觉)升级。

## 2. 实时数据与交互增强
- [ ] **Websocket/SSE 实时推送新邮件数量**：
  - **后端能力**：利用现有的 SSE (Server-Sent Events) 通道或建立新的 WebSocket (`wssocks`) 连接，监听 `email_bodies` / `email_summaries` 的新入库事件。
  - **前端能力**：当后台（或定时任务）抓取到新的邮件并完成大模型摘要时，向浏览器前端实时推送更新数量的提醒消息（例如在页面右下角或顶部弹出提示框：“发现 5 封新邮件，点击刷新”），从而免去用户手动硬刷新的麻烦。
